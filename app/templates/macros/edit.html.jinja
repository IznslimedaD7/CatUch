{% macro edit(url, id) -%}
    <form class="text-center inline-block cursor-pointer"
        action="{{ url_for(url, id=id) }}" method="get">
            <button type="submit">
                edit
            </button>
    </form>
{%- endmacro %}